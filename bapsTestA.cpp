#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    while (n--)
        if (n)
            printf("Hello World!\n");
        else
            printf("Hello World!\n\n");

    return 0;
}